Install ``barcode_stock`` and ``product_multi_barcode``. The module installs
itself automatically.

Give a product several barcodes through ``product_multi_barcode``. Inside the
warehouse app, scanning any of them in a picking now resolves to the same move
as the main barcode.
